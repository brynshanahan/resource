// import createStore from "zustand";
// import produce from "immer";

// const immerStore = config => (set, get, api) =>
//   config(fn => set(produce(fn)), get, api);

// export const [usePageStore, pageStoreApi] = createStore(
//   immerStore(set => ({
//     pages: {},
//     setField: (path, value) =>
//       set(state => {
//         state[path] = value;
//       })
//   }))
// );
